# Lokale Claude-Berechtigungen (maschinenspezifisch)
.claude/settings.local.json

# Build-Artefakte (liegen normalerweise in ~/Library/Caches/arduino)
build/
*.bin
*.elf
*.map

# WLAN-Zugangsdaten (lokal, niemals einchecken)
secrets.h

# macOS
.DS_Store
