#pragma once
// Vorlage: nach secrets.h kopieren und eigene WLAN-Daten eintragen.
//   cp secrets.h.example secrets.h
// secrets.h ist per .gitignore ausgeschlossen und landet nie im Repo.
#define WIFI_SSID "MeineSSID"
#define WIFI_PASS "MeinPasswort"
